# Package Availability

## Result

Blocked. The required packages are not available from a reproducible NuGet source currently configured for this repository.

## Configured sources

`dotnet nuget list source` returned only:

| Source | URL | Enabled | Requires credentials for this check |
| --- | --- | --- | --- |
| `nuget.org` | `https://api.nuget.org/v3/index.json` | Yes | No |

No repository `NuGet.Config`/`nuget.config` file was found.

## NuGet.org flat-container checks

Checked on 2026-08-02:

| Package | URL | Result |
| --- | --- | --- |
| `CSF.Analyzers.Architecture` | `https://api.nuget.org/v3-flatcontainer/csf.analyzers.architecture/index.json` | `404 Not Found` |
| `CSF.Analyzers.Reliability` | `https://api.nuget.org/v3-flatcontainer/csf.analyzers.reliability/index.json` | `404 Not Found` |
| `CSF.Analyzers.Testing` | `https://api.nuget.org/v3-flatcontainer/csf.analyzers.testing/index.json` | `404 Not Found` |

## NuGet.org search checks

Commands/checks:

```powershell
dotnet package search CSF.Analyzers --source https://api.nuget.org/v3/index.json --take 20
```

Result:

```text
No results found.
```

NuGet search API package ID checks returned `totalHits=0` for:

- `CSF.Analyzers.Architecture`
- `CSF.Analyzers.Reliability`
- `CSF.Analyzers.Testing`

## Analyzer repository documentation

The CSF.Analyzers documentation says NuGet.org publication is not enabled yet. Packages are generated by the release workflow and attached to GitHub Releases, but release-attachment consumption is not an accepted source for this repository because it would be a floating GitHub Release URL unless a stable documented feed is added.

## Decision

Do not alter:

- `Directory.Packages.props`
- `src/Directory.Packages.props`
- `tests/Directory.Packages.props`
- `tools/Directory.Packages.props`
- any `.csproj`
- any `packages.lock.json`
- `.editorconfig`

The adoption can be retried after the packages are published to NuGet.org or after the owner configures a documented reproducible feed that works without hidden credentials for template consumers.

## Version and hash

No package version or package hash is recorded because no package was resolved from an accepted feed.
